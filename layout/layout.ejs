<!DOCTYPE html>
<html>
<head>
    <meta charset='UTF-8'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <meta http-equiv='X-UA-Compatible' content='ie=edge'>
    <title>
        <%- config.title %>
        <%if (page.title != null && page.title.length > 0) { %>
        - <%- page.title %>
        <% } %>
    </title>

    <link rel='stylesheet' href='/layout.css'>

    <style>
        html {
            --primary-color: <%= theme.primaryColor %>;
            --secondary-color: <%= theme.secondaryColor %>;
            --text-light-color: <%= theme.textLightColor %>;
            --text-dark-color: <%= theme.textDarkColor %>;
            --background-light-color: <%= theme.backgroundLightColor %>;
            --background-dark-color: <%= theme.backgroundDarkColor %>;
        }
    </style>
</head>
<body>
    <% console.log(theme) %>
    <% if(!is_home()) { %>
        <a href='/' class='home-button'>
            <i class='icon-home'></i>
        </a>
    <% } %>

    <%- partial('partial/menu') %>

    <%- body %>

    <footer>
        <p>Blog feito com HTML, CSS, <a href='https://hexo.io' target='_blank'>Hexo</a>, <i class="icon-heart"></i> e pipoca com café</p>
        <p>Curiosidade de como o blog foi feito? <a href='https://github.com/jonathan-santos/pipocacafeinada' target='_blank'>Acesse o repositório do projeto</a></p>
    </footer>
</body>
</html>